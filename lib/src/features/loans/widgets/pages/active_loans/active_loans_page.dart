import 'package:flutter/widgets.dart';

class ActiveLoansPage extends StatelessWidget {
  const ActiveLoansPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
