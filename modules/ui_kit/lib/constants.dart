library constants;

import 'package:flutter/widgets.dart';

const kRadiusS = Radius.circular(8);
const kRadiusM = Radius.circular(12);
const kRadiusL = Radius.circular(16);
const kRadiusXL = Radius.circular(24);
